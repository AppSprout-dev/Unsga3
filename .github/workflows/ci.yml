name: ci

on:
  push:
    branches: [main, master]
  pull_request:
    branches: [main, master]

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: "10.0.x"

      - name: Restore
        run: dotnet restore Unsga3.slnx

      - name: Build
        run: dotnet build Unsga3.slnx -c Release --no-restore

      - name: Test
        run: dotnet test Unsga3.slnx -c Release --no-build --verbosity normal --logger "console;verbosity=detailed"

      - name: Pack
        run: dotnet pack src/Unsga3/Unsga3.csproj -c Release --no-build -o artifacts

      - name: Upload nupkg
        uses: actions/upload-artifact@v4
        with:
          name: nupkg
          path: artifacts/*.nupkg
          if-no-files-found: error

  typesafe-pareto:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Mock tests
        run: python -m unittest tools/typesafe-pareto/test_score_pareto.py -v

      - name: Smoke (mock)
        run: python tools/typesafe-pareto/score_pareto.py --smoke --force-mock --metrics metrics/typesafe-runs.jsonl
